services:
  api:
    build: .
    container_name: johanlijffijt-dev-api
    restart: unless-stopped
    user: "1001:1001" # johan's host UID:GID — keeps api/data/*.ndjson readable without sudo/docker exec
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - ./data:/app/data
