{
  "name": "johanlijffijt-dev",
  "private": true,
  "description": "Test-gate voor de rekenkundige logica en linkintegriteit van de Tools Hub (geen build-pipeline voor de site zelf, zie CLAUDE.md).",
  "scripts": {
    "test": "node scripts/verify-site.js && node --test tests/*.test.js"
  }
}
